# Bound the documentation toolchain to compatible major versions so a new
# upstream major release cannot break release-day Pages builds.
sphinx>=7.4,<9
myst-parser>=3.0,<5
sphinx-rtd-theme>=2.0,<4
sphinxcontrib-mermaid>=0.9,<2
sphinx-copybutton>=0.5,<1
